# Ulysses documents (for 'On This Mac').
export ULYSSES_PATH="$HOME"/Library/Containers/com.soulmen.ulysses3/Data/Documents/Library

# ia Writer documents (iCloud).
# Use with quotes: `$ cd "$IAWRITER_PATH"`
export IAWRITER_PATH="$HOME"/Library/Mobile\ Documents/74ZAFF46HB~jp~informationarchitects~Writer/Documents

# Da dots. 
export DOTS_PATH="$HOME"/.dotfiles

# Dev projects.
export DEV_PATH="$HOME"/dev

# Aliases for cd-ing.
# These don't work right.
# alias dots="cd $DOTS_PATH"
# alias dev="cd $DEV_PATH"